steps:
  # Install dependencies
  - name: 'python:3.9'
    entrypoint: pip
    args: ['install', '-r', 'requirements.txt', '--user']

  # Copy necessary JSON files to a staging directory
  - name: 'ubuntu'
    entrypoint: bash
    args:
      - '-c'
      - |
        mkdir -p /workspace/staging
        cp bildungsrechtsentscheide/*.json /workspace/staging/
        cp flaskapp.py /workspace/staging/
        cp requirements.txt /workspace/staging/

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'bildungsrecht-api'
      - '--source'
      - '/workspace/staging'
      - '--region'
      - 'europe-west1'  # Specify your desired deployment region
      - '--allow-unauthenticated'
      - '--platform'
      - 'managed'

timeout: '1600s'

options:
  logging: CLOUD_LOGGING_ONLY
  region: 'us-west2'  # Specify a region without quota restrictions
